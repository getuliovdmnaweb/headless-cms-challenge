## Product Context
See PRD.md, design.md, and tasks.md for requirements, architecture, and backlog.

This is a time-boxed take-home challenge (target ~4 hours). Testing is full TDD per standing convention, but suite depth is weighted toward the validator/schema-evolution logic over simple CRUD/UI — see the "Trade-offs" section of design.md.
